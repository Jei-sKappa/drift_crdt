name: drift_crdt
description: Dart implementation of Conflict-free Replicated Data Types (CRDTs), backed by Drift.
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  crdt: ^5.0.0
  drift: ^2.9.0

dev_dependencies:
  build_runner: ^2.4.13
  drift_dev: ^2.9.0
  test: ^1.26.3
  very_good_analysis: ^8.0.0
